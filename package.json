{
  "scripts": {
    "start": "vuepress dev",
    "build": "vuepress build",
    "preserver": "npm run build",
    "server": "http-server .vuepress/dist -p 1989",
    "predeploy": "npm run build",
    "deploy": "gh-pages -b master -d .vuepress/dist"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "vuepress": "^1.3.1"
  },
  "dependencies": {
    "dayjs": "^1.8.21"
  }
}
